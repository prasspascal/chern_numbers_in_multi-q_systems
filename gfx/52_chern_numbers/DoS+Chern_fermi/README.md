# Full Chern hierarchy

Fig. 5.4 is `./plots/limacon_Chernhierarchy_kpm.png` which was generated in the Jupyter notebook file `./limacon_DoS+Chern_fermi_kpm.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1746537187', '1746286805', '1746308778'` and were produced using the python scripts `/code/1D/run_theta_kpm.py` and `/code/1D/run_chern_fermi.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Fig. 5.5 is `./plots/skx_Chernhierarchy_kpm_0shift0mag.png` which was generated in the Jupyter notebook file `./skx_DoS+Chern_fermi_kpm_0shift0mag.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1700658946', '1746717241', '1746779749'` and were produced using the python scripts `/code/2D/run_theta_kpm.py` and `/code/2D/run_chern_fermi.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Fig. 5.6 is `./plots/sin_Chernhierarchy_kpm_0shift0mag.png` which was generated in the Jupyter notebook file `./sin_DoS+Chern_fermi_kpm_0shift0mag.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1725117202', '1746837632', '1746896165'` and were produced using the python scripts `/code/2D/run_theta_kpm.py` and `/code/2D/run_chern_fermi.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Fig. 5.10 is `./plots/sdw_Chernhierarchy.png` which was generated in the Jupyter notebook file `./sdw_DoS+Chern_fermi_kpm.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1746356686', '1746954269', '1747011839'` and were produced using the python scripts `/code/2D/run_theta_kpm.py` and `/code/2D/run_chern_fermi.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Tbl. 5.1 a) is `./tables/limacon_ChernNumber_Table.txt` and Tbl. 5.1 b) is `./tables/limacon_ChernCoefficient_Table.txt` which were generated in the Jupyter notebook file `./limacon_Chern_Hierarchy.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1746179416','1746179418','1746179420','1746179422','1746179424','1746179426'` were produced using the python script `/code/1D/run_chern.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Tbl. 5.2 a) is `./tables/skx_ChernNumber_Table.txt` and Tbl. 5.2 b) is `./tables/skx_ChernCoefficient_Table.txt` which were generated in the Jupyter notebook file `./skx_Chern_Hierarchy.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1726240275','1726240820','1726241356','1726241890','1726242428','1726242968'` were produced using the python script `/code/2D/run_chern.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Tbl. 5.3 a) is `./tables/sin_ChernNumber_Table.txt` and Tbl. 5.3 b) is `./tables/sin_ChernCoefficient_Table.txt` which were generated in the Jupyter notebook file `./sin_Chern_Hierarchy.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1746714501','1746714865','1746715217','1746715569','1746715924','1746716282','1746716641','1746717002'` were produced using the python script `/code/2D/run_chern.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Tbl. 5.4 a) is `./tables/sdw_ChernNumber_Table.txt` and Tbl. 5.4 b) is `./tables/sdw_ChernCoefficient_Table.txt` which were generated in the Jupyter notebook file `./sdw_Chern_Hierarchy.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1746717349','1746717698','1746718049','1746718402'` were produced using the python script `/code/2D/run_chern.py`.
The respective input parameters were recorded in `./data/*/params.json`.