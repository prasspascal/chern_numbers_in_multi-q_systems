# Creating figures for Section 6.2.

Fig. 6.4 a) and Fig. 5.2 b) are `./plots/flux_DOS.png` and `./plots/flux_IDS.png` which were generated in the Jupyter notebook file `./flux_IDS.ipynb`.
The corresponding data sets can be found in `./data/1709893028` and were produced using the python script `/code/2D/run_flux.py`.
The respective input parameters were recorded in `./data/1709893028/params.json`.

Fig. 6.4 c) and Fig. 5.2 d) are `./plots/flux_DOS_ferro.png` and `./plots/flux_IDS_ferro.png` which were generated in the Jupyter notebook file `./flux_IDS_ferro.ipynb`.
The corresponding data sets can be found in `./data/1709902764` and were produced using the python script `/code/2D/run_flux.py`.
The respective input parameters were recorded in `./data/1709902764/params.json`.

Fig. 6.5 is `./plots/flux_Chernhierarchy_kpm_morb.png` which was generated in the Jupyter notebook file `./flux_DoS+Chern+Morb_fermi_kpm_ferro.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1710151112', '1747815794'` and were produced using the python scripts `/code/2D/run_flux_kpm.py` and `/code/2D/run_chern_fermi.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Fig. 6.6 a) is `./plots/flux_DoS_kpm_skx.png` which was generated in the Jupyter notebook file `./flux_DoS_kpm_skx.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1710151112','1710239585','1709931786','1710305481','1710080177'` and were produced using the python script `/code/2D/run_flux_kpm.py`.
The respective input parameters were recorded in `./data/*/params.json`.

Fig. 6.6 b) is `./plots/skx_DoS_kpm_flux.png` which was generated in the Jupyter notebook file `./skx_DoS_kpm_flux.ipynb`.
The corresponding data sets can be found in `./data/*` with `*` from `'1700658946','1710239438','1710322977','1710355824','1710388814'` and were produced using the python script `/code/2D/run_theta_kpm.py`.
The respective input parameters were recorded in `./data/*/params.json`.